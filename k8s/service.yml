apiVersion: v1
kind: Service
metadata:
  name: drdcyudyt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "drdcyudyt"
    port: 8080
  selector:
    app: drdcyudyt
